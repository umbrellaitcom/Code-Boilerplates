import { createStyles } from '@material-ui/core';

export const styles = () =>
  createStyles({
    snackbarRoot: {
      whiteSpace: 'pre-line',
    },
    snackbarVariantError: {
      color: '#fff',
    },
  });
